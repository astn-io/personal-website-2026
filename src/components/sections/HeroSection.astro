---
type Props = {
  title: string;
};

const { title } = Astro.props;
---

<section>
  <h1>{title}</h1>
  <p>Hello, world.</p>
</section>

<style>
  section {
    display: flex;
    flex-direction: column;
    align-items: start;
    justify-content: center;

    min-height: 100svh;
  }
</style>
